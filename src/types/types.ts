export interface Dog {
  id: string;
  name: string;
  breed: string;
  age: number;
  img: string;
  zip_code: string;
} 

export interface Selection {
  key?: string;
  value?: string;
  page?: number;
}